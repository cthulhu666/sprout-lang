type Maybe a =
  | Just a
  | Nothing

fn map(f: a -> b, m: Maybe a) -> Maybe b =
  match m with
  | Just x -> Just(f(x))
  | Nothing -> Nothing

fn inc(x: Int) -> Int = x + 1

fn main() -> Unit !{IO} =
  print(map(inc, Just(2)))
