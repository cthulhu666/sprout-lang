type Maybe a =
  | Just a
  | Nothing

fn map(m: Maybe a, f: a -> b) -> Maybe b =
  match m with
  | Just x -> Just(f(x))
  | Nothing -> Nothing

fn inc(x: Int) -> Int = x + 1

fn main() -> IO Unit =
  print(map(Just(2), inc))
