.idea/
__pycache__/
.pytest_cache/
tmp/
*.pyc
notes.md
build/
retros/

# Dev screenshots from the graphics backend (SPROUT_GFX_SCREENSHOT).
sprout_shot.png

# Local Codeberg workflow config (see .codeberg.config.example).
# Contains your repo coordinates and tea config path — not secret, but
# host-/user-specific. Token itself stays in the tea config file.
.codeberg.config
