# A `where` constraint on a constructor with NO `exists` prefix would constrain a
# head parameter (Haskell's deprecated DatatypeContexts). Rejected at parse time.
type Foo a = | Bar a where ToString a
