fn is_even(x: Int) -> Bool =
  (x / 2) * 2 == x

fn double(x: Int) -> Int = x + x

fn add(acc: Int, x: Int) -> Int = acc + x

fn main() -> IO Unit =
  print(fold(map(filter(split_ints("1 2 3 4 5"), is_even), double), 0, add))
