module main

# `List File` is not itself linear, so before containment no obligation attached
# to `xs` and the File inside was dropped silently.

type linear File = File Int

fn go() -> Int =
  let xs = [File(1)]
  in 7

fn main() -> Unit !{IO} = print(to_string(go()))
