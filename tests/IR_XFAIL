# tests/IR_XFAIL — files that fail under --use-ir-codegen during M3 milestone.
#
# Format: one path per line, optional `# reason` after the path.
# Lines starting with `#` are comments. Blank lines ignored.
#
# When a file is removed from this list, the corresponding M3 PR has closed
# its parity gap. CI's test-ir job uses this to compute pass/fail:
#   - file in XFAIL + fails  → counted as xfail (OK)
#   - file in XFAIL + passes → "unexpected OK" (remove from XFAIL)
#   - file not in XFAIL + fails → unexpected failure (CI fails once gate is blocking)
#
# Validate this file with: bash scripts/check_ir_xfail.sh
#
# Closed gates (M3 milestone, in order):
#   PR 2 — CPR ctor lowering (boxed)              ✓ #47
#   PR 3 — TUnary `!` + `-`, binary `/`           ✓ #48
#   PR 4 — short-circuit `&&` / `||`              ✓ #49
#   PR 5 — top-level let bindings (module-init)   ✓ #50
#   PR 6 — top-level fn eta wrappers              ✓ #51
#   PR 7 — tuple expressions + TuplePattern       ✓
#   PR 8 — nested constructor patterns            ✓
#   PR 9 — String equality `==` / `!=`            ✓
#   PR 10 — Bool and Unit as i64 call args        ✓
#   PR 11 — do-block + IO + main body + Bool/Char ==  ✓ M3 corpus milestone closer
#            Cleared: hello, factorial, fizzbuzz, maybe_map, tuples, smoke_shapes 01/02/05
#   PR 12 — Char literal + char_to_string + CharPattern  ✓
#   PR 13 — do-block Maybe wrapper-strip binding (Result deferred)  ✓
#   PR 14 — user `extern fn` declarations emitted as LLVM declare  ✓ this PR
#            Cleared 56 entries: many were stale-labelled "@char_to_string"
#            or "@append" but actually stopped at the extern-declare gap.
#            Now: bytes / regex / time / process / int_range / env_get /
#            term_read_line / http_* / typeclass_*  all flow through.
#   PR 15a — Bool ctor field zext wiring (IRZextI1ToI64)  ✓ this PR
#             Cleared 3 entries: json_demo, sentry_api,
#             test_constrained_container_dispatch.
#             2 entries shifted to new blockers (see below).
#   PR 15b — WrapDecl ctor registration in ast_to_ir (build_ctor_table_acc,
#             translate_match_arm_ctor, translate_boxed_ctor_call)  ✓ this PR
#             0 entries fully cleared; 5 entries shifted to new blockers:
#               test_wrap_type      → translate_do drops sealed blocks from non-last
#                                     do steps (IR invalid: undefined arm_0_N predecessor)
#               test_ir_codegen_*x4 → do-block bind to tuple pattern not yet supported
#   PR 15c — do-block bind to TuplePattern  ✓ this PR
#             4 entries shifted to new blocker:
#               test_ir_codegen_*x4 → nested StringPattern in constructor match
#                                     (these tests compile stdlib.compiler which contains
#                                     StringPattern in ctor matches via bundler/iface_codec)
#   PR 15d — StringPattern in match arms (top-level + nested ctor + nested tuple)  ✓ this PR
#             Cleared 2 top-level StringPattern entries (repl_hosted, repl_navigation)
#             — both shifted to new blockers (see below).
#             5 nested-StringPattern entries shifted to Unit ctor field blocker.
#             1 new xfail added: test_ir_codegen_string_pattern.spr (same Unit ctor
#             field blocker as the other test_ir_codegen_* files).
#             list_pattern_demo shifted to nested BoolPattern (StringPattern cleared).
#   PR 15e — Unit ctor field as i64 0 sentinel  ✓ this PR
#             5 test_ir_codegen_* entries unblocked from Unit ctor field error.
#             All 5 + 1 new test (test_ir_codegen_unit_ctor_field) shifted to
#             pre-existing '%else_27' undefined-value bug (match-arm label/scope gap
#             in stdlib.compiler IR; same family as '%body_*' gap).
#   PR 15f — thread eblocks in translate_do non-last branches  ✓ this PR
#             10 entries cleared: test_ir_codegen_{closures,ctors,match,nested_patterns,
#             string_pattern,unit_ctor_field}, test_json, test_set, test_wrap_type,
#             ref_tutorial.
#             test_repl_navigation shifted to @analysis_check_source undefined
#             (same blocker as repl_hosted; translate_do eblocks gap cleared).
#   PR 15g — nested IntPattern + BoolPattern in ctor + tuple match  ✓ this PR
#             2 entries cleared:
#               list_pattern_demo    → nested BoolPattern in ctor (StringPattern was PR 15d)
#               test_return_type_dispatch → nested IntPattern in ctor match
#             test_ir_codegen_nested_int_bool passes IR verification (no new xfail needed).
#   PR 15h — TRange + compiler intrinsic declares  ✓ this PR
#             3 entries cleared:
#               int_range_demo       → TRange not yet supported (ast_to_ir)
#               repl_hosted          → @analysis_check_source undefined
#               test_repl_navigation → @analysis_check_source undefined
#   PR 16  — @append Semigroup parity (ast_to_ir translate_direct_call)  ✓ this PR
#             1 entry cleared:
#               tests/smoke_shapes/03_strconcat.spr → @append undefined
#             M3 IR-codegen feature parity COMPLETE after this PR.
#             All remaining xfail entries are pre-existing direct-codegen bugs
#             (dup-block 't0') or pre-existing inference/lowering correctness
#             defects (__unresolved_Eq__/__unresolved_ToString__) — outside the
#             M3 IR-codegen feature-parity scope.
#   PR 16.1 — analysis_* declare ABI fix (regression caught by PR 1.5
#             differential).  PR 15h declared analysis_* with i64 args but the
#             C runtime takes const char* (= ptr) and direct codegen's intrinsic
#             table uses ll_ptr().  Fixed to ptr; differential check on PR 70
#             now reports 0 unallowed divergences.
#   C      — streaming per-function IR codegen  ✓  (BACKLOG §1)
#            8 test_ir_codegen_* un-skipped: peak RSS ~2 GB → ~240 MB.
#
# Bucket breakdown after streaming IR codegen fix (BACKLOG §1):
#    ~8 entries — IR codegen OOM — RESOLVED.  The 8 test_ir_codegen_* tests
#                 that previously peaked at 2–2.6 GB under --use-ir-codegen now
#                 compile at ~240 MB via the streaming ir_pipeline.sprout
#                 orchestrator (translate→root→lower per-function, no full
#                 IRProgram materialized).  Removed from this list.
#    't0' dup-block class — RESOLVED (PR 11 item 2a).  Source params named
#                 `t<N>` collided with synthetic `%t<N>` temps on the typed path.
#                 Fixed by hardcoding the "%t$" prefix in ast_to_ir.fresh_name +
#                 ir_rooting.prefix_for_fn ('$' is reserved, so synthetic temps
#                 are disjoint from all source names).  astar/nqueens removed.
#    ~3 entries — typeclass `__unresolved_Eq__` / `__unresolved_ToString__`
#                 (typeclass dispatch gap — out of M3 IR-codegen scope)
#    ~2 entries — pre-existing direct-codegen xfails (unresolved name in source)

# === examples/ ===
# aoc_2025_day_1/2 cleared by the P11-2e init-globals idx_map fix (was the
# 't0' root-slot collision); removed from xfail.
examples/sentry_issue_browser.sprout           # pre-existing direct-codegen xfail (unresolved name in source)
examples/sentry_issue_browser_tui.sprout       # pre-existing direct-codegen xfail (unresolved name in source)

# === tests/stdlib/ ===
# deriving_eq_parametric / deriving_to_string / eq_operator_adt_dispatch cleared:
# typed codegen now null-fills the `__unresolved_<Class>` inner-dict sentinel
# that lowering emits for free-type-var dicts (provably never invoked), matching
# direct codegen.  See ast_to_ir.sprout TVar fallthrough.
# test_string / test_to_string cleared by the P11-2e init-globals idx_map fix
# (was the 't0' root-slot collision); removed from xfail.
