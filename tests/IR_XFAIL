# tests/IR_XFAIL — files that fail under --use-ir-codegen during M3 milestone.
#
# Format: one path per line, optional `# reason` after the path.
# Lines starting with `#` are comments. Blank lines ignored.
#
# When a file is removed from this list, the corresponding M3 PR has closed
# its parity gap. CI's test-ir job uses this to compute pass/fail:
#   - file in XFAIL + fails  → counted as xfail (OK)
#   - file in XFAIL + passes → "unexpected OK" (remove from XFAIL)
#   - file not in XFAIL + fails → unexpected failure (CI fails once gate is blocking)
#
# Validate this file with: bash scripts/check_ir_xfail.sh
#
# PR-assignment legend (see /Users/cthulhu/.claude/plans/witty-brewing-wolf.md):
#   PR 2  — CPR ctor lowering (boxed)              — CLOSED
#   PR 3  — TUnary `!` + `-`, binary `/`           — CLOSED (this PR)
#   PR 3b — Bool zext, top-level let (residual)
#   PR 4  — Tuple literals + TuplePattern
#   PR 5  — do-block + IO effects + main body lowering
#
# Bucket breakdown after PR 3 closes (re-probed):
#   72 entries — short-circuit `&&` / `||` (next dominant blocker; needs
#                separate scope decision — eager or short-circuit lowering)
#    8 entries — PR 5 do-block / IO / main body
#    2 entries — PR 3b top-level let (unbound 'r1' surfaces after `/` cleared)
#    1 entry  — PR 4 TuplePattern in match
#    2 entries — pre-existing direct-codegen xfails (unresolved names in source)

# === examples/ ===
examples/aoc_2025_day_1.sprout                 # PR 3b: top-level let (unbound 'r1')
examples/aoc_2025_day_2.sprout                 # PR 3b: top-level let (unbound 'r1')
examples/aoc_2025_day_3.sprout                 # short-circuit `&&` / `||` (next blocker — needs scope decision)
examples/aoc_2025_day_4.sprout                 # short-circuit `&&` / `||` (next blocker)
examples/aoc_2025_day_5.sprout                 # short-circuit `&&` / `||` (next blocker)
examples/astar.sprout                          # short-circuit `&&` / `||` (next blocker)
examples/collections_demo.sprout               # short-circuit `&&` / `||` (next blocker)
examples/do_notation_demo.sprout               # short-circuit `&&` / `||` (next blocker)
examples/factorial.sprout                      # PR 5: do-block + IO + main body
examples/fizzbuzz.sprout                       # PR 5: do-block + IO + main body (was PR 8 div; / cleared)
examples/foldable_demo.sprout                  # short-circuit `&&` / `||` (next blocker)
examples/hello.sprout                          # PR 5: do-block + IO + main body
examples/http_echo_server.sprout               # short-circuit `&&` / `||` (next blocker)
examples/http_get_cli.sprout                   # short-circuit `&&` / `||` (next blocker)
examples/int_range_demo.sprout                 # short-circuit `&&` / `||` (next blocker)
examples/io_do_demo.sprout                     # short-circuit `&&` / `||` (next blocker)
examples/io_result_do_demo.sprout              # short-circuit `&&` / `||` (next blocker)
examples/json_demo.sprout                      # short-circuit `&&` / `||` (next blocker)
examples/list_pattern_demo.sprout              # short-circuit `&&` / `||` (next blocker)
examples/maybe_map.sprout                      # PR 5: do-block + IO + main body
examples/modules_demo.sprout                   # short-circuit `&&` / `||` (next blocker)
examples/nqueens.sprout                        # short-circuit `&&` / `||` (next blocker)
examples/process_demo.sprout                   # short-circuit `&&` / `||` (next blocker)
examples/ref_tutorial.sprout                   # short-circuit `&&` / `||` (next blocker)
examples/ref_union_find.sprout                 # short-circuit `&&` / `||` (next blocker)
examples/regex_demo.sprout                     # short-circuit `&&` / `||` (next blocker)
examples/repl_hosted.sprout                    # short-circuit `&&` / `||` (next blocker)
examples/result_demo.sprout                    # short-circuit `&&` / `||` (next blocker)
examples/sentry_api.sprout                     # short-circuit `&&` / `||` (next blocker)
examples/sentry_issue_browser.sprout           # pre-existing direct-codegen xfail (unresolved name in source)
examples/sentry_issue_browser_tui.sprout       # pre-existing direct-codegen xfail (unresolved name in source)
examples/string_templates.sprout               # short-circuit `&&` / `||` (next blocker)
examples/tcp_echo_once.sprout                  # short-circuit `&&` / `||` (next blocker)
examples/tcp_echo_server.sprout                # short-circuit `&&` / `||` (next blocker)
examples/text_demo.sprout                      # short-circuit `&&` / `||` (next blocker)
examples/tuples.sprout                         # PR 4: TuplePattern in match
examples/typeclass_collections_demo.sprout     # short-circuit `&&` / `||` (next blocker)
examples/typeclass_functor_foldable_demo.sprout  # short-circuit `&&` / `||` (next blocker)

# === tests/smoke_shapes/ ===
tests/smoke_shapes/01_noparam.spr              # PR 5: do-block + IO + main body
tests/smoke_shapes/02_adtmatch.spr             # PR 5: do-block + IO + main body
tests/smoke_shapes/03_strconcat.spr            # PR 5: do-block + IO + main body
tests/smoke_shapes/04_generic.spr              # short-circuit `&&` / `||` (next blocker)
tests/smoke_shapes/05_lambdaparams.spr         # PR 5: do-block + IO + main body
tests/smoke_shapes/06_tuple_param.spr          # short-circuit `&&` / `||` (next blocker)

# === tests/stdlib/ ===
tests/stdlib/test_collections.spr              # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_compute_eta_fwd_markers.spr  # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_constrained_container_dispatch.spr  # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_constrained_eta_codegen.spr  # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_dbe_synthetic.spr            # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_deriving_eq.spr              # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_deriving_eq_parametric.spr   # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_deriving_ord.spr             # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_deriving_to_string.spr       # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_eq_operator_adt_dispatch.spr # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_fn_compose.spr               # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_instance_method_subst.spr    # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_ir_codegen_arith.spr         # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_ir_codegen_basic.spr         # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_ir_codegen_calls.spr         # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_ir_codegen_closures.spr      # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_ir_codegen_control.spr       # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_ir_codegen_ctors.spr         # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_ir_codegen_match.spr         # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_ir_codegen_strings.spr       # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_ir_rooting.spr               # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_json.spr                     # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_lexer_slice.spr              # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_list_pattern_runtime.spr     # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_math.spr                     # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_mutvec.spr                   # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_native_constrained_maybe_helper.spr  # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_native_fold_indexed.spr      # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_process.spr                  # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_regex.spr                    # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_repl_navigation.spr          # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_return_type_dispatch.spr     # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_set.spr                      # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_str_slice_bytes.spr          # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_string.spr                   # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_time.spr                     # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_to_string.spr                # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_toplevel_let_closure.spr     # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_tuple_tostring.spr           # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_typeclass_laws.spr           # short-circuit `&&` / `||` (next blocker)
tests/stdlib/test_wrap_type.spr                # short-circuit `&&` / `||` (next blocker)
